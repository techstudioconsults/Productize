import { Box } from '@chakra-ui/react';
import ActiveUserPage from './layout/AdminRevenue';

export const AdminRevenue = () => {
    return (
        <Box mt={8}>
            <ActiveUserPage />
        </Box>
    );
};
