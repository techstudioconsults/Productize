import { Box } from '@chakra-ui/react';
import { ProfileForm } from '../../lib/dashboard-profile/ProfileForm';

export const Profile = () => {
  return (
    <Box my={8}>
      <ProfileForm />
    </Box>
  );
};
