import { Box } from '@chakra-ui/react';
import { ProfileForm } from '@productize/dashboard-lib/ui';
import React from 'react';

const Profile = () => {
  return (
    <Box my={8}>
      <ProfileForm />
    </Box>
  );
};

export default Profile;
